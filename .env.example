# Port to run the server on
PORT=8000

# Secure 32-character encryption key for AES-256-GCM. 
# Generate one using: node -e "console.log(crypto.randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_secure_32_character_encryption_key_here
